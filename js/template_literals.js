// TEMPLATE LITERALS
const name = "Javier";
const profession = "Software engineer";
// String concatenation with back-tick (`)
console.log(`${name}, ${profession}\n`);
// Use of templates inside strings
const text = `Hi there!\nMy name is ${name} and I'm ${profession}`;
console.log(text);
